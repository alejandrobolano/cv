import {Component, OnInit} from '@angular/core';

@Component({
  selector: 'ambm-authentication',
  template: `<router-outlet></router-outlet>`
})
export class AuthenticationComponent implements OnInit {

  constructor() {
  }

  ngOnInit(): void {
  }
}
